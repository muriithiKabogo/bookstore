<h3>Add New Category</h3>

<%form for @category do |f|%>
	<div class="form-group">
		<%=f.label :name%><br/>
		<%=f.textfield :name, class: "form-control"%><br/>
	</div>

	<%=f.submit 'Submit', class: "btn btn-primary"%>
	<%= link_to 'Cancel', categories_path, class: "btn btn-default"%>
<%end%>
